node_modules/
www/*
# Hand-written bootstrap + offline pages live in www/ — keep them tracked even
# though the rest of www/ is build output we don't commit.
!www/index.html
!www/offline.html
.DS_Store

# Android build outputs
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/captures/
android/.idea/
android/*.iml
android/app/*.iml

# Signing — never commit
android/app/release.keystore
android/app/keystore.properties
android/app/upload-keystore.jks
*.jks

# Generated
*.apk
*.aab
*.log
